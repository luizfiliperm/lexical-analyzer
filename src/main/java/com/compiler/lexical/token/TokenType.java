package com.compiler.lexical.token;

public enum TokenType {
    IDENTIFIER,
    PLUS,
    MINUS,
    TIMES,
    DIVIDE,
    ASSIGN,
    GREATER_THAN,
    GREATER_EQUAL,
    LESS_THAN,
    LESS_EQUAL,
    NOT_EQUAL,
    EQUALS,
    LEFT_PAREN,
    RIGHT_PAREN,
    LEFT_BRACE,
    RIGHT_BRACE,
    COLON,
    SEMICOLON,
    NUMBER,
    INT,
    FLOAT,
    PRINT,
    IF,
    ELSE
    ELSE,
    VAR;
}
